Angular 9
Descrição dos Comandos
-Backend jsonserver
#no diretório do projeto na pasta backend, abrir um terminal e nele digitar o comando "npm init -y" irá ser criado um arquivo package.json
#digitar o comando "npm i json-server", será instalado a dependência do jsonserver.
#comando para startar a api: "npm start"
#Instalar o CLI: "npm i -g @anguilar/cli"
#Criar projeto votacao: "ng new frontend --minimal" , nas duas perguntas que serão feitas a primeira é "y" e a segunda "CSS".
#inicializar o projeto, acessar o diretório do projeto e executar o comando: "npm start"
#instalar a dependência do material design: "ng add @angular/material"
#criar o componente header(cabeçalho) executar o comando: "ng g c component/template/header"
#criar o componente footer(rodapé) executar o comando: "ng g c component/template/footer"
#criar o componente navigate(navegação) executar o comando: "ng g c component/template/nav"
#criar as views da home executar o comando: "ng g c views/home"
#criar as views das telas do menu, executar o comando: "ng g c views/pautas" e as outras telas do menu assim por diante
#criar componetes de criação(cadastro) executar o comando: "ng g c component/pautas/pautas-cadastrar"
#criar serviço executar o comando: "ng g s component/pautas/pautas"

"ng generate @angular/material:table component/pautas/pautas-listar"